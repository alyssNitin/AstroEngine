# NarayanAstroReader — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic[email]>=2.0.0
python-multipart>=0.0.9

# AI
anthropic>=0.28.0

# Vedic astrology engine (PyJHora dependencies)
pyswisseph>=2.10.3
numpy>=1.26.0
geopy>=2.4.0
timezonefinder>=6.5.0
geocoder>=1.38.0
pytz>=2024.1

# Database — PostgreSQL
psycopg2-binary>=2.9.9

# Authentication — JWT + PII encryption
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0

# Structured JSON logging
structlog>=24.1.0

# 2FA / TOTP
pyotp>=2.9.0

# Session store / rate limiter
redis>=5.0.0

# Report generation
fpdf2>=2.7.9
python-docx>=1.1.0

# AWS KMS (optional — uncomment if using KMS key management)
# boto3>=1.34.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
